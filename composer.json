{
	"name": "mdaashir/foodzy",
	"type": "project",
	"license": "MIT",
	"authors": [
		{
			"name": "mdaashir",
			"email": "s.mohamedaashir@gmail.com"
		},
		{
			"name": "Mirudhulabashini N",
			"email": "22pt16@psgtech.ac.in"
		}
	],
	"require": {
		"ext-pgsql": "*",
		"vlucas/phpdotenv": "^5.6"
	},
	"scripts": {
		"start": "php -S localhost:8000",
		"install": "composer install --prefer-dist --no-progress",
		"install-prod": "composer install --prefer-dist --no-dev --no-progress --optimize-autoloader"
	},
	"autoload": {
		"psr-4": {
			"Foodzy\\": "src/"
		}
	}
}
